# 设计交叉评审报告：<变更名称>

<!-- 时机：module-designs 全部完成后、tasks 设计开始前。
     评审对象：design.md（总）+ design/<模块>.md（全部），对照 proposal.md 与 specs/。
     方式：同一条消息并行启动 5 个子 agent——
     4 个维度评审组（R1 对齐与决策 / R2 契约与数据 / R3 防御 / R4 质量属性）
     + 1 个走查验证组（W：数据流转模拟 + 场景推演含失败变体）。
     通过标准：CRITICAL/HIGH 清零且复审通过，评审结论写为"通过"后，
     才允许生成 tasks.md / tasks/<模块>.md。 -->

## 评审结论

- 结论：**待评审 / 通过 / 不通过**
- 当前轮次：N / 3（保险丝：最多 3 轮）
- 评审对象清单：design.md、design/<模块1>.md、design/<模块2>.md …

## 维度覆盖对照

| # | 维度 | 负责组 | 结论（通过/问题数） |
|---|------|--------|---------------------|
| 1 | 需求符合性 | R1 | |
| 2 | 架构合理性 | R1 | |
| 3 | 接口契约 | R2 | |
| 4 | 数据设计 | R2 | |
| 5 | 健壮性/可靠性 | R3 | |
| 6 | 安全 | R3 | |
| 7 | 性能与扩展性 | R4 | |
| 8 | 可运维性（学习项目轻量评估） | R4 | |
| 9 | 可测试性 | R2 | |
| 10 | 可维护性/可演进性 | R4 | |
| 11 | 业务闭环性 | W（推演验证） | |
| 12 | 决策与风险 | R1 | |

## 问题统计

| 严重度 | 发现数 | 已修复 | 遗留（CRITICAL/HIGH 须为 0 才能通过） | 忽略理由 |
|--------|--------|--------|----------------------------------------|----------|
| CRITICAL | | | | 不允许忽略 |
| HIGH | | | | 不允许忽略 |
| MEDIUM | | | | 不修复须写明理由 |
| LOW | | | | 可忽略 |

> 升级规则：同一位置/同一问题被 ≥2 个组命中时，严重度自动升一级（最高 CRITICAL）。

## R1 对齐与决策 —— 问题清单

<!-- 维度：1 需求符合性 / 2 架构合理性 / 12 决策与风险 -->

| 编号 | 严重度 | 归属维度 | 位置（文件§章节） | 问题描述 | 修复建议 | 状态 |
|------|--------|----------|------------------|----------|----------|------|
| R1-1 | | | | | | 待修复/已修复/复审通过 |

## R2 契约与数据 —— 问题清单

<!-- 维度：3 接口契约 / 4 数据设计 / 9 可测试性 -->

| 编号 | 严重度 | 归属维度 | 位置（文件§章节） | 问题描述 | 修复建议 | 状态 |
|------|--------|----------|------------------|----------|----------|------|
| R2-1 | | | | | | |

## R3 防御 —— 问题清单

<!-- 维度：5 健壮性/可靠性 / 6 安全 -->

| 编号 | 严重度 | 归属维度 | 位置（文件§章节） | 问题描述 | 修复建议 | 状态 |
|------|--------|----------|------------------|----------|----------|------|
| R3-1 | | | | | | |

## R4 质量属性 —— 问题清单

<!-- 维度：7 性能与扩展性 / 8 可运维性 / 10 可维护性/可演进性 -->

| 编号 | 严重度 | 归属维度 | 位置（文件§章节） | 问题描述 | 修复建议 | 状态 |
|------|--------|----------|------------------|----------|----------|------|
| R4-1 | | | | | | |

## W 走查验证组 —— 问题清单

<!-- 主责维度 11 业务闭环性；推演中发现的其他维度问题也登记在此，标注归属维度 -->

| 编号 | 严重度 | 归属维度 | 位置（文件§章节） | 问题描述 | 修复建议 | 状态 |
|------|--------|----------|------------------|----------|----------|------|
| W-1 | | | | | | |

### 数据流转模拟记录

<!-- 每个核心业务场景（正常/边界/异常各至少 1 条）构造具体示例数据，
     沿设计链路走一遍：API 入参 → 服务处理 → 表落库/变更 → 下游模块，
     逐步记录数据形态变化。发现的问题登记到上方 W 清单。 -->

#### 模拟 1：<场景名>（正常 / 边界 / 异常）

- 示例数据：`{ "字段": "合成示例值" }`
- 走查路径：`POST /api/... → XxxService#方法 → 表 xxx（INSERT/UPDATE）→ 下游模块`
- 每步数据变化：
  1. 入参：
  2. 服务层处理后：
  3. 落库后（关键列的值）：
  4. 下游读取到的数据：
- 结论：链路走通 / 状态到达终态 / 发现缺陷（问题编号 W-x）

### 场景推演记录（含失败变体）

<!-- 取 specs 的典型用例过一遍设计（桌面检查）；
     每个核心场景至少推演一个失败变体：中途失败 / 重复提交 / 依赖不可用 / 并发冲突 -->

#### 推演 1：<用例名>

- 正常路径推演：<逐步对照设计走查，结果>
- 失败变体：<中途失败 / 重复提交 / 依赖不可用 / 并发冲突>
  - 推演过程：
  - 设计给出的行为：
  - 结论：可恢复、状态闭环 / 发现缺陷（问题编号 W-x）

## 越界发现与汇总仲裁

<!-- 各组"其他发现"（HIGH+ 的非本组维度问题）归并记录；
     ≥2 组命中同一问题的升级记录；组间结论冲突的主流程仲裁理由 -->

| 来源组 | 问题摘要 | 归并到 | 处理（去重/升级/仲裁结论） |
|--------|----------|--------|----------------------------|
| | | | |

## 修复记录

<!-- 每轮修复登记一条；设计文档的修改由主流程统一执行，评审子 agent 只输出报告 -->

| 问题编号 | 轮次 | 修改文件与位置 | 修复内容摘要 | 复审组 | 复审结果 |
|----------|------|----------------|--------------|--------|----------|
| | | | | | 复审通过/仍有问题 |
